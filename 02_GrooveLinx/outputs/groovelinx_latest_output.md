# GrooveLinx — Global Action Badge + Notification Architecture

_Generated by Claude · 2026-03-24_

## Summary

Promoted FeedActionState to global action engine. Added personal action badge on Feed nav, notification classification layer, push architecture, and app icon badge prep. Fixed critical unreachable-code bug in feed-action-state.js.

## Files Changed

| File | What |
|------|------|
| `js/core/feed-action-state.js` | Fixed unreachable voteOnPoll(), added notification classification, preferences, push prep, nav badge state, app icon badge |
| `js/features/band-feed.js` | Nav badge sync on load + every action, background badge refresh every 2min |
| `js/ui/gl-left-rail.js` | Added glRailFeedBadge element on Feed nav item |

## Badge

- Count = `needsMyInput` (personal action debt only)
- Visible on Feed nav item in left rail (yellow dot)
- Updates instantly after: inline vote, resolve, archive, tag change, note
- Background refresh: 4s after app load, then every 2min (polls only, lightweight)
- `navigator.setAppBadge()` on Chrome/Edge PWA; graceful no-op on iOS
- Zero = hidden

## Notification Architecture

| Component | Status |
|-----------|--------|
| Classification (ACTION_REQUIRED / CRITICAL_CHANGE / FYI) | Implemented |
| Push eligibility check | Implemented |
| Push payload format | Implemented |
| Preference model (localStorage) | Implemented, defaults: action=ON, critical=ON, fyi=OFF |
| Permission request flow | Implemented |
| Service worker push handler | Already existed |
| Backend push delivery (VAPID) | Not started — needs server |
| iOS PWA push | Architecture ready — needs VAPID server |
| Settings UI for preferences | Data model ready — needs UI |

## Structural Fix

`feed-action-state.js` had TWO return statements. `voteOnPoll()` was defined between them — unreachable dead code. Inline voting appeared to work because the second return block included it, but the first return was actually what executed. Rewrote with single return block.

## Previous Session (same day)

- Feed Action State engine created (centralized ownership/completion/badges)
- Band Feed v5 consuming it as single source of truth
- Left rail badge identity fix (email prefix → display name)
- Inline poll voting (no navigation required)
- Dashboard status filter: removed songs excluded from all practice/readiness surfaces
- Feed workflow hardening: personal vs band input split, persistent return context
- "Loading weak songs..." hang fixed
- Comprehensive status filter audit (7 locations fixed)
- P0 blank screen fix (boot watchdog, auth path gaps)
- PWA install/update notification system
- App icon fix (full-bleed, no white padding)

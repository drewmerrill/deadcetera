# GrooveLinx — Session Output: 2026-03-24 → 2026-03-25

_Generated by Claude · 2026-03-25_

## Session Summary

Massive feature sprint: 7 new files created, 13 files modified, ~30 commits.
Evolved GrooveLinx from a collection of tools into a guided band operating system.

---

## KEY DELIVERABLES

### 1. GrooveMate (Avatar Guide)
Contextual guide that evolves from Fan → Bandmate → Coach.

- **Engine**: `gl-avatar-guide.js` — 15 rule-based triggers, intent layer, Next Best Action
- **UI**: `gl-avatar-ui.js` — floating 🎸 button + right-side slide-in panel
- **Name**: GrooveMate
- **Universal CTA**: "▶ Run What Matters" (adapts to context)
- **Auto-launch**: ≥3 songs → Play dashboard → "Nice — let's run one. Hit play."
- **Magic moment**: first completion → "That already sounded tighter."
- **Tip cap**: max 2/day with cooldowns

### 2. Unified Player Engine
Single player system for all playback surfaces.

- **Engine**: `gl-player-engine.js` — state machine with 4s terminal guarantee
- **Resolver**: `gl-source-resolver.js` — curation + YouTube/Spotify/Archive
- **UI**: `gl-player-ui.js` — overlay/float/bar + completion screen
- **Confidence**: "✔ Playing: Song Title · YouTube · Best available version"
- **Completion**: reflection + streak + band signal + next actions

### 3. Spotify Web Playback SDK
Full-track in-app playback for Premium users.

- **Module**: `gl-spotify-player.js` — SDK lifecycle + device management
- **Scopes**: streaming, user-read-playback-state, user-modify-playback-state
- **Fallback**: SDK → embed iframe → external open
- **iOS**: "Tap play to start Spotify on this device"

### 4. Band Mode (wired, not rebuilt)
Play dashboard + Go Live + float audio.

- Play dashboard now has Next Action + Scorecard (same as Sharpen)
- Go Live: 🎧 button toggles float audio over charts
- Bidirectional sync: chart nav ↔ audio player
- Quick notes in Go Live saved to Firebase

### 5. Band Scorecard
"Are we getting better as a band?"

- Health headline + coach line (human language)
- Top Focus callout
- "✔ What's Working" / "▶ Focus Here"
- Rating dots + trend
- Song movement (locked in / need attention / in progress)

### 6. Rehearsal System
Complete lifecycle with summaries + mixdowns.

- Session summary screen: rating, reflection, notes, mixdown
- Headline insights per session
- Delete + bulk delete for past sessions
- Mixdown archive: upload, playback, Chopper integration
- Trend indicator + mixdown tagging

### 7. Band Feed Cleanup
Delete, post types, pin to Band Room.

- Post types: note, link, photo
- Pin/unpin to Band Room
- Edit posts (inline)
- Single + bulk delete (creator/admin)
- System filter + auto-post suppression

### 8. Progression Tracking
Action → Practice → Improvement → Next Action loop.

- Action log (14-day localStorage)
- Completion-aware Next Action Card
- Practice streaks + milestones
- Band activity signals + momentum visual
- Top Songs to Work card

---

## SUPPORTING FILES

> **ChatGPT**: Check these files in `02_GrooveLinx/outputs/groovelinx_supporting_files/` for additional context:

| File | Contents |
|------|----------|
| `band_mode_audit.md` | Architecture audit of all player/overlay systems + screenshot capture guide |
| `session_2026-03-25_full_manifest.md` | Complete file manifest: all files created, modified, commits, Firebase paths |

---

## BUGS FIXED

| Bug | Root Cause | Fix |
|-----|-----------|-----|
| Song picker crash | `_slPickerShowLibrary` undeclared in strict mode | Declared variable + Show Library toggle |
| Setlist player race condition | Concurrent launch() shared closure state | Launch token guard + full reset |
| Wrong setlist header (Wings Café) | slPlaySetlist re-sorted but idx was unsorted | Removed re-sort |
| Spotify popup blocked | window.open after async lost gesture context | Synchronous open before await |
| Back to Feed hidden | z-index 900 behind nav header | Raised to 1100 + safe-area |
| Spotify/Archive embeds not rendering | Engine didn't emit embedReady for non-YouTube | Added emit for all sources |
| "Opening best version" hung forever | Fallback had no terminal state | Replaced with actionable UI |

---

## ARCHITECTURE

```
GrooveMate (Avatar)
  └── Intent Layer → Next Best Action → Universal CTA

GLPlayerEngine (Unified Player)
  └── GLSourceResolver → YouTube / Spotify SDK / Spotify Embed / Archive
  └── GLPlayerUI → Overlay / Float / Bar / Completion

Band Scorecard
  └── Health + Coach + Strengths + Issues + Song Movement

Rehearsal System
  └── Plan → Active → Summary → Save → History
  └── Mixdowns → Upload / Playback / Chopper

Band Feed
  └── Notes / Links / Photos / Polls / Ideas
  └── Pin to Band Room / Edit / Delete
```

---

## NEXT PRIORITIES

1. Test GrooveMate avatar flow end-to-end (new band experience)
2. Test unified player across setlists + listening bundles
3. Test Spotify SDK with Premium account
4. Test Go Live + float audio sync
5. Wire curation UI (choose version / set North Star / reset to auto)
6. Decommission legacy setlist-player.js

---

## AUTO-ENGAGE MODE POLISH (Latest Update)

### Changes applied:
1. **No auto-dismiss** — overlay stays until user clicks Run or Skip
2. **No click-on-dim dismiss** — only explicit buttons close
3. **De-emphasized exit** — "Skip for now" in smaller, lower-contrast text (was "I'll explore first")
4. **CTA sub-text** — "We picked the best songs for you to work on right now" below Run What Matters
5. **Mid-playback toast** — "🔒 You're locking in already" after 25 seconds of active playback
6. **Smarter magic moment** — names the weakest song: "'Scarlet Begonias' still needs reps — hit that next?"
7. **No magic auto-dismiss** — user must choose Play or Skip
8. **Avatar suppressed** through auto-engage AND magic moment, restored on dismiss

---

## AUTO-ENGAGE MODE (Previous Update)

First-session focused overlay when ≥3 songs reached:
- Background dims with blur (click-to-dismiss)
- Avatar button hidden during focused state
- Centered card: "Nice — let's run one. Just hit play — I've got the rest."
- Single CTA: "▶ Run What Matters" (gradient, prominent)
- Secondary: "I'll explore first" (dismisses cleanly)
- Auto-dismisses after 20 seconds
- Avatar button restored on dismiss
- No competing CTAs visible during this state

Language pass complete: zero instances of "Want to", "You can", "Try" remaining in guidance.

> **ChatGPT**: See `groovelinx_supporting_files/session_2026-03-25_full_manifest.md` for complete file manifest.

---

## CRITICAL BUG FIX: Non-Active Songs in Top Songs to Work

**Root cause**: `_getWeakSongs()` used `if (st && !activeStatuses[st]) return;` — songs with `null`/`undefined` status (never explicitly set) passed through because `null && anything` is falsy.

**Fix**: Changed to `if (!st || !activeStatuses[st]) return;` — songs MUST have an explicit active status to appear. No status = excluded.

**Scope**: 3 instances in home-dashboard.js (lines 519, 611, 1222) all fixed with `replace_all`. Affected:
- `_getWeakSongs()` → Top Songs to Work card
- `_countWeakSongs()` → Next Action Card weak song count
- Scorecard readiness analysis → song movement counts

**Full codebase audit**: setlists.js and practice.js already use the correct pattern (return false for null status). The broken pattern only existed in home-dashboard.js.

> **ChatGPT**: See `groovelinx_supporting_files/session_2026-03-25_full_manifest.md` for complete file manifest.

---

## SCREENSHOTS NEEDED

> **DREW**: Capture these screenshots and save to `02_GrooveLinx/outputs/groovelinx_supporting_files/`:

1. `play_dashboard.png` — Play mode home dashboard (with Next Action + Scorecard)
2. `groovemate_button.png` — GrooveMate 🎸 floating button
3. `groovemate_panel.png` — GrooveMate slide-in panel with guidance
4. `completion_screen.png` — Completion screen after finishing a queue
5. `live_gig_audio.png` — Go Live with 🎧 float player active
6. `band_scorecard.png` — Band Scorecard card on dashboard
7. `rehearsal_summary.png` — Session summary screen after rehearsal
8. `mixdown_card.png` — Rehearsal mixdown card with playback

These are needed for ChatGPT to verify visual state before making UI recommendations.

# GrooveLinx Reality Audit — Index

**Tracking issue:** [#30 GrooveLinx Reality Audit](https://github.com/drewmerrill/deadcetera/issues/30)
**Workflow:** Plan → **Audit** → Simplify → Stabilize → Ship
**Rule:** Read-only inventory. No fixes-as-you-go. Each audit produces a numbered report; cleanup is scheduled as separate work after Drew + ChatGPT review.

---

## Audits

| # | Topic | Date | Status | Key findings | Report |
|---|---|---|---|---|---|
| 01 | System Inventory | 2026-05-12 | ✅ Complete | 1 verified-dead file (`home-dashboard-cc.js`), 33 direct Firebase reads bypassing GLStore, 7 inline `ACTIVE_STATUSES` shadows, player contract advisory-only, `version.json` never read, `app.js`/`app-dev.js` no sync enforcement, 2 half-built modules (workbench, bulk-import) | [GROOVELINX_REALITY_AUDIT_01_SYSTEM_INVENTORY.md](./GROOVELINX_REALITY_AUDIT_01_SYSTEM_INVENTORY.md) |
| 02 | Data Access Inventory | 2026-05-12 | ✅ Complete | 156 Firebase reads + 125+ direct writes catalogued; **5 subscribed listeners with no `.off()` cleanup** (gl-leader.js:250, band-feed.js x3, calendar.js:3918); **W1 setlist whole-array write at app.js:11882 re-opens 2026-05-10 SWR-clobber vector**; rehearsal-sessions (30+ reads) and band feed (20+) have no canonical owner; 7 Spotify calls bypass worker + connect wrapper; 62 of 68 localStorage keys lack `_glSafeCache` envelope; 6+ orphan keys; zero cross-tab sync; 46 worker routes all live (no dead routes) | [GROOVELINX_REALITY_AUDIT_02_DATA_ACCESS.md](./GROOVELINX_REALITY_AUDIT_02_DATA_ACCESS.md) |
| 03 | Page-Level Coverage Map | 2026-05-13 | ✅ Complete | 28 routes + 5 overlays inventoried; **5 ownership conflicts** (rehearsal_sessions w/5 writers; songs w/15+ readers + 5 writers; setlists w/6 writers; polls w/3 writers; practice_tasks w/no clear owner); **5 leaked resources** (feed 5-min interval; home visibilitychange listener; song-detail drift timer; pocket-meter mic stream; rehearsal GLStore subscription); 5 inline-in-app.js renderers (admin, venues, tuner, metronome, equipment, contacts); 1 verified orphan DOM (`stoner`); workbench still HALF-BUILT; 6 convergence candidates proposed (C1 player surface, C2 GLStore.RehearsalSession, C3 chart contract, C4 status badge, C5 GLBandFeedStore, C6 per-route lifecycle hook in showPage) | [GROOVELINX_REALITY_AUDIT_03_PAGE_COVERAGE.md](./GROOVELINX_REALITY_AUDIT_03_PAGE_COVERAGE.md) |
| 04 | Player / Audio / Playback Architecture | 2026-05-13 | ✅ Complete | **~13 audio surfaces, ~5 orchestration layers.** Contract layer (Phases C.1–C.4 in `gl-player-contract.js` + 3 adapters) already shipped — C1 unification is mostly DONE at the spec level. **3 big gap classes:** (1) Lifecycle — `GLPlayerEngine`, `SetlistPlayer`, `GLSpotifyConnect.stopPolling`, `harmony-lab`, `bestshot` are NOT registered with `GLRouteLifecycle`; only `song-detail` Stems, `pocket-meter`, and (today) `rehearsal-session` are. (2) Direct Spotify API — `listening-bundles.js:761,976,982` bypass both `gl-spotify-connect.js` and the worker. (3) No `pauseAll()` — harmony-lab, bestshot, setlist-player, Stems mixer, app.js memory loops can all play simultaneously. **iOS-specific:** SDK is intentionally unusable per `gl-spotify-connect.js:6–10`; Connect path is mandatory. No `pagehide`/`freeze` handlers. `_deadceteraAudioCtx` duplicated in app.js × 2 sites (`:4663` + `:9011`). **No SYSTEM LOCK violations** — no `GL_PAGE_READY`, `focusChanged`, or `ACTIVE_STATUSES` piercing in player code. **C1 timing:** the contract layer is done; what remains is lifecycle integration (Stab-scale work, not Convergence Initiative scale). Audit recommends **Stab #06 — Player Lifecycle Integration** as the next move (10 lifecycle disposers, ~75 LOC, low risk) before launching C1 Phase 2. | [GROOVELINX_REALITY_AUDIT_04_PLAYER_ARCHITECTURE.md](./GROOVELINX_REALITY_AUDIT_04_PLAYER_ARCHITECTURE.md) |
| 05 | Dead Code + Orphan Flow Audit | 2026-05-13 | ✅ Complete | **1 verified-dead file** (`home-dashboard-cc.js` — HTML comments confirm prior removal intent, zero call sites). **2 archive-folder dead files** (`archive/ARCHIVED_learning_resources.js`, `02_GrooveLinx/docs/chart-audit-snippet.js`). **1 dead config entry** (`_glPageScripts['rehearsal-mode']` at `navigation.js:337` — file is loaded eagerly, lazy entry never fires). **3 dead branches/comments** (`navigation.js:575` `if (false)`, `home-dashboard.js:1492/2788` `// REMOVED` comment blocks). **2 deprecated null shims** (`SD_LENSES_BY_MODE`, `GL.MODE_PAGES/MODE_LANDING`). **1 router bug** (`workbench` in `_HASH_VALID_PAGES` but not `VALID` restore array). **1 orphan writer** (`bulk-import.js:182` `chart_url` — no reader). **Reclassifications:** `workbench.js` is EXPERIMENTAL not HALF-BUILT (10+ programmatic callers, no nav entry); `playback-session.js` is ACTIVE BUT DUPLICATIVE with `gl-now-playing.js`. **Verified-LIVE despite earlier suspicions:** `pocket-meter.js`, `band-comms.js`, `song-pitch.js`, `plLoadIndex()`, all 4 avatar-feedback modules, all 4 rehearsal compute engines. **No orphan window functions or onclick handlers found with high confidence** — lazy-load architecture creates many false positives from naive grep. **Recommended first cleanup commit:** items D1, D4, D5, D6, D7 (≈30-50 LOC, −24 KB, zero behavior change). | [GROOVELINX_REALITY_AUDIT_05_DEAD_CODE_ORPHAN_FLOW.md](./GROOVELINX_REALITY_AUDIT_05_DEAD_CODE_ORPHAN_FLOW.md) |
| 06 | Stale Client / Service Worker / Update UX | 2026-05-13 | ✅ Complete | **Foundation is sound.** SW uses right strategy per resource class (network-first for `index.html` + `version.json` with timeouts; cache-first w/ bg refresh for JS/CSS; cache-first w/ no-cors for CDN). `skipWaiting()` + `clients.claim()` both wired. Two parallel update detection paths (SW byte-change + version.json content compare) converge on a single per-version banner (P0.4 sticky-dismiss fixed). **3 risk areas:** (1) **No `visibilitychange`/`pageshow` hook** triggers `checkForAppUpdate()` — iOS PWA backgrounded tabs can stay stale for hours past a deploy because the 5-min interval pauses while frozen. (2) **5 unversioned CSS files** (`styles.css`, `app-shell.css`, `rehearsal-mode.css`, `version-hub.css`, `css/gl-shell.css`) — SW install pre-cache mostly compensates but leaves a Cloudflare/HTTP-cache race window. (3) **`controllerchange` auto-reloads unconditionally** — risks mid-rehearsal disruption. **~60 cached-shell fallback branches** (typeof X === undefined guards on RehearsalSession × 9, _glNormalizeRefTitle × 9, GLSpotifyConnect × 10+, ChartRenderer × 6, GLPlayerContract.pauseAll × 6, GLRouteLifecycle × 4, GLNotes × 8, PracticeSession × 7, STATUS_LABELS × 2, isActiveSong × 2) — all classified Necessary; pattern is consistent and works. **1 dead-code finding:** `_loadedVersion === '0'` skip guard at `app.js:13061` is unreachable (default fallback is `''`, never `'0'`). **Recommended Stab #09:** add `visibilitychange`/`pageshow` → `checkForAppUpdate()`; suppress `controllerchange` auto-reload during `rehearsal-mode`/live-gig; remove dead `'0'` check. ≤50 LOC across `app.js`+`app-dev.js`, low risk. **No SYSTEM LOCK violations.** | [GROOVELINX_REALITY_AUDIT_06_STALE_CLIENT_UPDATE_UX.md](./GROOVELINX_REALITY_AUDIT_06_STALE_CLIENT_UPDATE_UX.md) |
| 07 | Module Size + Decomposition Criteria | — | ⏸ Pending | Files >5,000 LOC: `rehearsal.js`, `calendar.js`, `home-dashboard.js`, `song-detail.js`, `app.js`. Define split criteria. Was previously slotted as #05 — renumbered after Audit #05 (Dead Code) took the slot per Drew's audit prompt. | — |
| 08 | Listener & Subscription Lifecycle (deep dive) | — | ⏸ Pending | Original Audit #04 scope was reframed to Player Architecture. The exhaustive interval/setTimeout/rAF/window-level-event inventory remains pending. Renumbered #06 → #08 after Audit #06 (Stale Client) took the slot per Drew's audit prompt. | — |

---

## Stabilization Fixes

| # | Title | Date | Build | What changed | Limits | Linked audit |
|---|---|---|---|---|---|---|
| 01 | W1 Setlist Clobber + Firebase Listener Cleanup | 2026-05-12 | `20260513-012027` | (1) **W1 fix:** band-creation flow at `app.js:11882` (+ mirror in `app-dev.js:11490`) no longer does a whole-array `firebaseDB.ref('bands/{slug}/setlists').set(wholeArray)` after a SWR read. Replaced with per-record write to `…/setlists/{nextKey}` with `updatedAt`/`updatedBy` stamps — same semantics as `saveBandArrayDataSafe`, inline because the band-creation flow targets an explicit slug rather than the current band path. (2) **Listener cleanup:** `band-feed.js` real-time polls + ideas listeners now store query refs + handler refs and expose `window._feedRealtimeTeardown()`; wired to `beforeunload`. `calendar.js` connection watcher now stores the handler ref and exposes `window._calUnwatchConnections()`; wired to `beforeunload`. (3) **False positive flagged:** `gl-leader.js:250` rehearsal_sync listener was already cleaned up properly via `_syncAttachListener` → `_syncDetachListener` (`.off()` called before re-subscribe). No change made there; the audit was wrong about it. | No schema migration. No architecture refactor. No GLStore convergence. No A2P file changes. Audit-tool writes (`gl-data-audit.js:344`), `gl-task-engine.js:392` snapshot restore, and `groovemate_tools.js:190/358` left untouched — flagged for a later focused pass. | [Audit #02](./GROOVELINX_REALITY_AUDIT_02_DATA_ACCESS.md) §3.3 (W1) + §2.2 (listeners) |
| 02 | Groovemate Setlist Write Safety | 2026-05-13 | `20260513-012353` | **Investigation:** the two flagged sites (`groovemate_tools.js:190` `addSongToSetlist` and `:358` `_createSetlistFromSongs`) were already guarded by `typeof window.saveBandSetlistsSafe === 'function'` — the unsafe `db.ref(_bp('setlists')).set(…)` was only-fallback for a load-order regression that doesn't occur in practice. Still, the silent-clobber regression risk made the code grep-match Audit #02's W1 pattern. **Fix:** replaced both fallback branches with a fail-loud `console.error` + early return. `addSongToSetlist` now returns `{success:false, retryable:true}`; `_createSetlistFromSongs` returns `null`. Happy path through `saveBandSetlistsSafe` is unchanged. **Verified by grep:** zero unsafe `db.ref(bandPath('setlists')).set` calls remain in user/AI-triggered code paths. **Untouched (per scope):** `gl-task-engine.js:392` (intentional undo/snapshot restore — already commented "Restore setlists to pre-task state"). | No schema. No refactor. No GLStore convergence. No A2P file changes. Only `groovemate_tools.js` modified. | [Audit #02](./GROOVELINX_REALITY_AUDIT_02_DATA_ACCESS.md) §3.3 (W1 follow-up) |
| 03 | Per-Route Lifecycle Hook | 2026-05-13 | `20260513-122512` | **Added `window.GLRouteLifecycle`** (register/leave/disposers/currentRoute) in `js/ui/navigation.js`. Wired into `showPage()` so the previous route's disposers run before any DOM changes. **Per-route disposers wired for the two ACTUAL leaks:** (a) `song-detail.js` — new `window._sdStemsCleanup` clears the 500ms drift `setInterval` + closes the AudioContext; registered for `songdetail`. (b) `app.js` + `app-dev.js` Pocket Meter mount path — registers a disposer for `pocketmeter` that calls `_pmInstance.destroy()` (releases mic stream + classifier interval + visibilitychange handler + rAF + Firebase listener). **Teardown capability added (NOT per-route)** for three session-wide handlers that the audit flagged but are intentionally session-long: `band-feed.js _feedBgBadgeRefresh` captures interval/timeout IDs and exposes `window._feedBgBadgeTeardown`; `home-dashboard.js` extracts the `visibilitychange` handler to a named ref + exposes `window._homeVisibilityTeardown`; `rehearsal.js` captures `GLStore.on('focusChanged')`'s returned unsubscribe + exposes `window._rhFocusTeardown`. All three remain session-wide (self-guarded by `currentPage === X` checks) — their teardown is reserved for future sign-out / `beforeunload` paths. **Audit-finding reconciliation:** of the 5 listeners flagged by Audit #03 §3, only 2 are actual per-route leaks; the other 3 were single-execution IIFE/module-load listeners with internal page guards (not stacking, not per-route). The report flagged them honestly and we added cleanup capability without behavior change. | No GLStore convergence. No player unification. No schema changes. No A2P file changes. `app.js` ↔ `app-dev.js` mirror kept in sync. | [Audit #03](./GROOVELINX_REALITY_AUDIT_03_PAGE_COVERAGE.md) §3 (listener lifecycle) + C6 |
| 04 | Status Display Centralization | 2026-05-13 | `20260513-133724` | **Premise correction first:** Audit #01 listed "7 inline ACTIVE_STATUSES shadows" but on inspection that count conflated three distinct patterns: (a) **load-order fallback guards** in `gl-focus.js:48` + `song_matching_engine.js:364` — these already route through `GL.ACTIVE_STATUSES` when available, the inline literal is only a defensive fallback; (b) **subset filters** in `home-dashboard.js` (7 sites: 3001/3606/3619/3631/4171/5271/5597) that use a 4-key `{prospect, learning, rotation, gig_ready}` — intentionally narrower than canonical, excludes legacy `wip`/`active` by design; (c) **display-label maps** in `songs.js` (3 sites: 217/382-383/860) — these ARE legitimate duplicates of each other but were NOT duplicates of `ACTIVE_STATUSES`. Also discovered `gl-status-badge.js` is the **connectivity indicator** (Live/Refreshing/Cached/Offline) — there is no canonical song-status badge component. **Fix:** added `GLStore.STATUS_LABELS`, `GLStore.STATUS_LABELS_EMOJI`, `GLStore.STATUS_COLORS` to `js/core/groovelinx_store.js` alongside `ACTIVE_STATUSES`; routed `songs.js:217/382/383/860` through them with the same defensive `(GLStore && GLStore.X) ? … : { …fallback… }` pattern. Visual behavior identical (canonical values copied verbatim from the inline maps). Annotated `home-dashboard.js` site 3001 + added file-header convention comment documenting why the 4-key subset is intentional. Left `gl-focus.js:48` + `song_matching_engine.js:364` untouched (already canonical-routed). **Governance:** rewrote `CANONICAL_SYSTEMS.md` Status Rendering section — corrected the wrong "owner = gl-status-badge.js" entry, added canonical owners for active set + display maps + colors, documented the home-dashboard subset exception, clarified gl-status-badge.js is NOT a song-status component. | No new badge component built (none existed; not in scope). No home-dashboard scoring changes (4-key subset preserved verbatim). No A2P file changes. No GLStore convergence beyond the 3 new display maps. | [Audit #01](./GROOVELINX_REALITY_AUDIT_01_SYSTEM_INVENTORY.md) §"7 ACTIVE_STATUSES shadows" finding |
| 05 | Chart Renderer Enforcement | 2026-05-13 | `20260513-151218` | **Inventory:** 12 chart-related surfaces inspected (4 displays, 6 data loads, 2 edit). Two phases were already done (B.1 song-detail Band lens, B.2 rehearsal-mode Chart Tab load) per `song_workbench_architecture_audit.md §8.4`. Most remaining surfaces cannot safely migrate to `ChartRenderer.renderHtml` without behavior change: `live-gig.js:_renderChartHTML` is a smart chord-segment parser (different functionality); `setlists.js:parachuteBuildHtml` is print-only HTML with its own CSS class; `workbench.js:_wbToggleChartMax` is an interactive fullscreen with transpose/auto-scroll/auto-fit-font; `app.js:renderChartSection` (+ `app-dev.js` mirror) is a 4-line muted preview with `overflow:hidden` and `padding` that canonical's API doesn't expose. **Migrated one surface:** `song-detail.js:467` Play Mode lens chart text now routes through `ChartRenderer.renderHtml({fontSize:15, lineHeight:1.8, letterSpacing:'0.02em', maxHeight:'none'})`. Required a small canonical API extension — added optional `letterSpacing` opt (defaulted to `'0.01em'`) and documented `maxHeight: 'none'` as the disable-scrolling sentinel. Side effect of migration: Play Mode now decodes HTML entities (was a bug — Band lens already decoded the same data, so users with stored `&amp;` literals saw them as `&amp;` in Play Mode but as `&` in Band lens). **Verification:** `grep "ChartRenderer\\." --include="*.js"` shows 5 canonical call sites (Band lens render + empty state, rehearsal Chart Tab multi-load, Play Mode render, new opt accept). Direct `<pre>` chart renders outside canonical: 1 in song-detail.js (legacy SW-shell fallback, intentional), 1 in app.js + 1 in app-dev.js (muted preview cards, intentional). Legacy fallback branches preserved verbatim — they exist so a cached service-worker shell without `gl-chart-renderer.js` still renders charts. **Governance:** rewrote `CANONICAL_SYSTEMS.md` Chart Rendering section with the full API surface, B.1–B.4 migration phases, intentional non-canonical surfaces list, and editing exceptions list. | No new renderer features built (canonical API got 1 new opt + 1 sentinel — no behavior change for existing callers). No edit-path migrations (out of scope). No schema/data changes. No A2P file changes. live-gig + setlists print + workbench fullscreen + 4-line previews left as documented exceptions. | [Audit #03](./GROOVELINX_REALITY_AUDIT_03_PAGE_COVERAGE.md) C3 (chart contract) |
| C2.1 | `GLStore.RehearsalSession` Canonical Ownership — Phase 1 | 2026-05-13 | `20260513-152155` | **Convergence Initiative C2, Phase 1.** Introduced `window.GLStore.RehearsalSession` as the canonical chokepoint for all Firebase access to `bands/{slug}/rehearsal_sessions/**`. New module: `js/core/gl-rehearsal-session.js` (~280 lines), pattern modeled on `gl-practice-session.js`. **API:** `loadAll / loadById / create / update / setField / remove / setCurrent / getCurrent / clearCurrent / subscribe / getStats`. **Auto-stamping:** `create`/`update`/`setField`-parent stamp `updatedAt = Date.now()` + `updatedBy = currentUserEmail` (matching `saveBandArrayDataSafe` semantics). **Defensive logging:** counters for reads/writes/removes/subscribes/duplicate-subscribe-attempts/currentlyOwned/activeSubs exposed via `getStats()`. **Lifecycle:** registers a `GLRouteLifecycle` disposer for `rehearsal` route + `beforeunload` defense-in-depth; tears down all active `.on()` subscriptions on route leave. Duplicate-subscribe detection: returns existing unsubscribe fn + bumps a counter rather than double-attaching. **Migrated 9 sites in Phase 1:** `js/features/rehearsal.js` lines 236 (create), 252 (update), 311 (setField audio_segments), 1762 (loadAll), 1774 (remove), 3613 (loadById), 3714 (mixdown-tag update); `rehearsal-mode.js` lines 1155 (create from session-end), 1488 (post-rating update). Each migration keeps the canonical+fallback shape: `(GLStore && GLStore.RehearsalSession.X) ? GLStore.RehearsalSession.X(…) : direct firebaseDB.ref(...) …` so a cached SW shell without the new module still works. **Deferred to Phase 2 (documented in C2_REHEARSAL_SESSION_MIGRATION_MAP.md):** 19 sites across `multitrack-rehearsal.js` (6), `recording-analyzer.js` (6), `rehearsal-analysis-pipeline.js` (4), `gl-insights.js` (1), `gl-rehearsal-scheduling.js` (1), `groovemate_tools.js` (1), `band-feed.js` (1). Of these, ~9 require new helpers (`loadField`, `setField`, `loadForBand`, `loadRecent`) not yet built. 2 calendar Drive-backed reads are out of scope (separate storage). 2 build-time scripts are permanent deferrals. **Governance:** `CANONICAL_SYSTEMS.md` Rehearsal Session State section rewritten with full API + Phase 1 site list + Phase 2 deferred list. `DATA_OWNERSHIP_RULES.md` Tier-2 row updated — `rehearsal_sessions` owner is now `GLStore.RehearsalSession` (was "proposed; not yet implemented"). `STABILIZATION_DASHBOARD.md` gained a Convergence Initiatives status block. New `02_GrooveLinx/audits/C2_REHEARSAL_SESSION_MIGRATION_MAP.md` is the canonical inventory + deferral rationale. | No schema migration. No rehearsal-system redesign. No UI rewrite. No GLStore rewrite. Did not touch multitrack-rehearsal complex logic. Did not touch recording-analyzer pipelines. Did not touch practice-heartbeat timing logic. No A2P file changes. Each migrated site keeps its pre-existing Firebase fallback as cached-shell safety. | [Audit #03](./GROOVELINX_REALITY_AUDIT_03_PAGE_COVERAGE.md) C2 |
| 06 | Player Lifecycle Integration | 2026-05-13 | `20260513-184757` | **Translated Audit #04 quick-wins into action.** Six surfaces now integrated with `GLRouteLifecycle` or defended with `beforeunload`. **Per-route disposers added:** (1) `setlist-player.js:_createOverlay` registers `close` for the route the overlay opened on — overlay closes on nav-away, queue + floating bar persist. (2) `harmony-lab.js:renderHarmonyLab` registers `_hlCleanup` for `songdetail` route — pauses split-mixer audios + take-review element when leaving Song Detail. (3) `bestshot.js:renderBestShotPage` registers `_bsCleanup` for `bestshot` route — pauses `chopAudio` + `chopAudioContext.suspend()` (suspend not close — preserves decoded buffers for re-entry). **Cross-route `beforeunload` defense (NOT per-route, by design):** (4) `gl-player-engine.js` calls `stop()` + `GLSpotifyConnect.stopPolling()` + closes `_deadceteraAudioCtx`. Engine plays cross-route intentionally via the floating now-playing bar, so pausing on every route would break that UX. (5) `gl-spotify-connect.js` calls `stopPolling()` defensively — engine ownership already coordinates polling via `gl-player-engine.js:340`'s `stop()`, but `beforeunload` covers the tab-kill path. **Audit #04 reconciliation:** the `_deadceteraAudioCtx` dedup that Audit #04 flagged as "duplicated at app.js:4663 + :9011" was ALREADY GUARDED with `if (!window._deadceteraAudioCtx)` at both sites in both app.js AND app-dev.js (4 sites total). No code change required for dedup; only the `beforeunload` close was missing (added in gl-player-engine.js). **Contract groundwork:** `GLPlayerContract.CAPABILITIES.PAUSE_ALL` constant declared in `gl-player-contract.js` with semantics doc — engines that declare this capability will pause themselves when another asserts ownership, once a `pauseAll(exceptEngine)` arbitrator is built. NOT yet implemented; this is groundwork only. | No player redesign. No playback unification. No UI work. No SDK/Connect routing changes. No schema changes. No A2P file changes. Each surface keeps its existing cleanup logic — disposers are pure additions that call into existing pause/close methods. | [Audit #04](./GROOVELINX_REALITY_AUDIT_04_PLAYER_ARCHITECTURE.md) §7 quick-wins 7.1–7.6, 7.10 (groundwork) |
| 07 | Global `pauseAll()` Playback Arbitration | 2026-05-13 | `20260513-190522` | **Built the cross-engine pause arbitrator that Stab #06 set up groundwork for.** `GLPlayerContract.pauseAll(exceptId)` is now live — single-owner playback is enforced by construction across 5 surfaces. **Arbitration core (`gl-player-contract.js`):** `pauseAll(exceptId)` walks two registries — engine adapters declaring `CAPABILITIES.PAUSE_ALL` (deduped by `engine.id`) + ad-hoc surfaces registered via new `registerPausable(id, pauseFn)` API. `_arbitrating` flag guards re-entrant calls (a misbehaving pause cannot trigger a runaway loop). Defensive try/catch per surface — a failing pause doesn't block the cascade. Compact logging only when something paused or failed. **Engine participation:** added `PAUSE_ALL` capability to gl-player-engine-contract.js, gl-setlist-player-contract.js, gl-stems-engine-contract.js (each adapter already had `pause()` so no new methods needed). **Assertion call sites (6):** `gl-player-engine.js:172` engine play, `setlist-player.js:527` launch, `song-detail.js:2717` Stems toggle play branch, `harmony-lab.js:599` split-mixer play, `harmony-lab.js:1308` take-review play, `bestshot.js:3104` delegated document `play` listener on #chopAudio (capture-phase — single hook covers all 5 internal chopAudio.play sites: spacebar/canvas/region/hotspot/preview). **Pausable registrations (2):** harmony-lab and bestshot register at module load via setTimeout retry pattern (matches `GLStore.PracticeSession` / `GLStore.RehearsalSession` precedent). **Excluded by design + documented:** app.js memory loops + multitrack nudge (4 transient base64 audios in scattered code paths — wrapping each is invasive, surfaces don't survive route change anyway), Spotify SDK/Connect transports (covered indirectly by GLPlayerEngine arbitration; arbitrating separately would double-pause), pocket-meter mic (input only, different audio direction). **New doc:** `02_GrooveLinx/KNOWN_STABLE_FLOWS.md` — trust-level matrix for all playback flows, route transitions, and arbitration participation. Marks Song Detail Stems + Harmony Lab + BestShot as **Needs iPhone verification** for Stab #07's new arbitration paths. | No player redesign. No transport-control changes. No queue-behavior changes. No SDK/Connect routing changes. No UI changes. No schema changes. No A2P file changes. All 9 modified files pass `node -c`. Recursion-guarded; logging-throttled. | [Audit #04](./GROOVELINX_REALITY_AUDIT_04_PLAYER_ARCHITECTURE.md) §4.1 (concurrent playback risk #1) + §7.10 (PAUSE_ALL groundwork from Stab #06) |
| 09 | Stale Client Resume Check + Rehearsal Reload Guard | 2026-05-13 | `20260513-204319` | **Translates Audit #06 findings into action.** Five additive changes across `app.js` + `app-dev.js` mirror, ≤50 LOC, no behavior change for foreground non-performance use. **(1) Visibility-driven update check.** Added `document.addEventListener('visibilitychange', ...)` that fires `checkForAppUpdate()` when `visibilityState === 'visible'`. Closes the iOS PWA backgrounded-tab gap — the 5-min `setInterval` pauses while iOS Safari freezes the tab, so a tab resumed after hours of freeze had no automatic poll until the next interval fire (could be up to 5 min away). 30s debounce via `_glLastVisUpdateCheck` timestamp prevents version.json spam from rapid tab-switching. **(2) Pageshow resume hook.** Added `window.addEventListener('pageshow', ...)` that fires `checkForAppUpdate()` when `event.persisted === true` (bfcache resume). Belt-and-suspenders for the visibilitychange path; some iOS resume paths fire pageshow but not visibilitychange. Same debounce. **(3) Performance-mode reload guard.** Modified the boot-time `controllerchange` listener at `app.js:545-549` (mirror at `app-dev.js:544-548`) — when the new SW takes control, instead of unconditional `location.reload()` it first checks `GLStore.isPerformanceMode()` (the canonical signal that's true during both rehearsal-mode AND live-gig, set via `GLStore.setAppMode('performance')` from `rehearsal-mode.js:469` + `live-gig.js:74` and reverted on close). If in performance mode, show the existing update banner instead. Normal pages keep auto-reload behavior. Closes the "page yanked from under the band mid-show" risk. **(4) Dead guard removed.** Deleted `if (_loadedVersion === '0') { ... return; }` at `app.js:13061` (mirror at `app-dev.js:12655`) — unreachable because `BUILD_VERSION` default fallback is `''`, never `'0'`. Vestigial from an earlier version system. **(5) Documentation:** `KNOWN_STABLE_FLOWS.md` updated with new "Update / Resume / Reload" section documenting the resume flows + reload guard semantics. `STABILIZATION_DASHBOARD.md` summary block updated. **Verification:** `node -c` passes on both app.js + app-dev.js + service-worker.js; `python3 -m json.tool` validates version.json; grep confirms visibilitychange + pageshow listeners in both files; grep confirms zero remaining `_loadedVersion === '0'` matches; build bump consistent across all 4 sources. **Held back (out of scope per Drew's prompt):** CSS cache-busting on the 5 unversioned hrefs (`styles.css`, `app-shell.css`, `rehearsal-mode.css`, `version-hub.css`, `css/gl-shell.css`) — separate small commit when convenient. Settings debug panel — defer. Force-reload on major version mismatch — explicitly NOT done (would risk mid-rehearsal disruption). | No service-worker rewrite. No cache architecture redesign. No banner behavior changes (per-version gating preserved). No forced reloads. `app.js` ↔ `app-dev.js` mirror kept in sync. No A2P file changes. No SYSTEM LOCK touches. | [Audit #06](./GROOVELINX_REALITY_AUDIT_06_STALE_CLIENT_UPDATE_UX.md) §7.2 recommendations 1–3 + §8 |
| Clean #1 | Reality Audit #05 verified-dead cleanup | 2026-05-13 | `20260513-201027` | **First cleanup commit from Audit #05.** Five low-risk removals, zero behavior change. **(D1)** Deleted `js/features/home-dashboard-cc.js` (file, 23 KB). Zero call sites; `index.html:760` + `index-dev.html:761` HTML comments already said `REMOVED — legacy Command Center monkey-patch`. The data contracts the file READ from (`window._lastPocketScore`, `_lastPocketTrend`) are still alive via `gl-rehearsal-recordings.js`, `gl-rehearsal-agenda.js`, `groovelinx_store.js`, `pocket-meter.js` — only the consumer file was dead. **(D4)** Removed `'rehearsal-mode': ['rehearsal-mode.js']` line from `_glPageScripts` (`js/ui/navigation.js:337`). `rehearsal-mode.js` is loaded eagerly at `index.html:770` + `index-dev.html:771` and `rehearsal-mode` is NOT in `_HASH_VALID_PAGES`, so the lazy entry could never fire. Removing the dead config entry. **(D5)** Deleted unreachable `if (false) { // dead code — kept for reference }` block at `navigation.js:575-576`. The `localStorage.removeItem('glLastSong')` on the preceding line still runs. **(D6)** Deleted 2-line `// REMOVED — were only called by the dead _renderSharpenDashboard function.` comment block at `home-dashboard.js:1492`. Pure documentation of a prior removal; no code. **(D7)** Deleted 2-line `// REMOVED — were dead code (line 375 always calls _renderLockinDashboard).` comment block at `home-dashboard.js:2788`. Same pattern. **Held back per scope (NOT touched in this commit):** Q1 `SD_LENSES_BY_MODE` shim, Q2 `GL.MODE_PAGES/MODE_LANDING` shims, Q3 `bulk-import.js:182` `chart_url` orphan writer, K1 `if (false)` retained gig-day logic at `home-dashboard.js:894`, K2 `workbench` router bug (`navigation.js` `VALID` array), K3 `playback-session.js ↔ gl-now-playing.js` duplication, U1 `workbench.js` no-nav-entry status, archive/ folder, all experimental systems. **Verified:** `home-dashboard-cc.js` absent from disk; zero `rehearsal-mode` matches in `navigation.js`; zero `if (false)` matches in `navigation.js`; zero `// REMOVED` matches in `home-dashboard.js`; all touched JS passes `node -c`. | This is a cleanup commit, not a stabilization fix — no new functionality, no convergence, no schema changes. No A2P file changes. Build bumped 4 sources atomically (`version.json`, `index.html`, `index-dev.html`, `service-worker.js`). The other 11 Audit #05 findings (Q1–Q4, K1–K3, U1, D2, D3, plus the dev-only archive/doc-folder items) intentionally untouched per Drew's scoped instruction. | [Audit #05](./GROOVELINX_REALITY_AUDIT_05_DEAD_CODE_ORPHAN_FLOW.md) §6 items D1, D4, D5, D6, D7 + §7.A safe-removal list |
| 08 | Spotify API Chokepoint + North Star Title Hydration | 2026-05-13 | `20260513-192327` | **Two concurrent fixes** — (A) Spotify API consolidation per Audit #04 §3, and (B) the user-visible North Star "Loading..." bug. **(A) Canonical Spotify API surface:** `GLSpotifyConnect.apiRequest(method, path, body?, opts?)` is now the chokepoint for every `api.spotify.com` call. Wraps existing internal `_req()` which already handles token refresh, 401 retry, 429 backoff, 5xx retry, transient network blip recovery. Opts: `legacyShape: true` (returns null on no-token / parsed error-body on non-ok — preserves listening-bundles' return contract), `silent: true` (swallow warnings for hydration paths). Companion `hasValidConnection({ bypassCache })` does a lightweight `/me` probe with 60s cache. **listening-bundles.js migrations:** `_checkAndStorePremium` (line 761 → migrated; legacy fallback at :769 retained for cached SW shell), `_spotifyApi` (line 968 → migrated as a thin shim; legacy fallback at :996/1001 retained). All 3 remaining `api.spotify.com` direct-fetch sites in the file are inside the `if (!GLSpotifyConnect)` cached-shell fallback branches per the existing convention. **(B) North Star "Loading..." bug:** **Root cause** found at `app.js:3330` + `app-dev.js:3310` — `title: title \|\| 'Loading...'` poisoned every new North Star record with the literal sentinel when the user didn't supply a title. If subsequent oEmbed hydration ever failed (rate-limited, network blip), the saved title stayed `'Loading...'` forever and every render site (`song-detail.js:788/4522/4580/4659`, `rehearsal-mode.js:2484`, `bestshot.js:60`, `gl-player-ui.js:575/649/676`) read it via `v.fetchedTitle \|\| v.title \|\| 'X'` chains. **Fix:** new save sentinel is platform-aware (`'Spotify Track'`, `'YouTube Video'`, etc.) — never `'Loading...'`. New `window._glNormalizeRefTitle(v, fallback)` helper in `js/core/utils.js` is the canonical title resolver — filters the legacy `'Loading...'` literal and applies platform fallback. All 9 display sites migrated through it. `fetchRefTrackInfo` upgraded: Spotify branch prefers `apiRequest('GET','/tracks/{id}')` when OAuth available (richer metadata: name + artist + album cover) → falls back to oEmbed → final fallback `'Spotify Track'`. `renderRefVersions` now persists hydrated `fetchedTitle` back to Firebase via `saveRefVersions` (only when fetch succeeded and value differs) — a single Listen-lens visit heals legacy `'Loading...'` records system-wide for all other consumers. **Verification:** 0 remaining `'Loading...'` literal saves in app.js or app-dev.js; 3 remaining `api.spotify.com` direct-fetch sites in listening-bundles, all in cached-shell fallback paths; all 7 modified JS files pass `node -c`. | No Spotify rewrite. No playback redesign. No North Star schema migration. No UI redesign. Cached SW-shell fallbacks preserved verbatim. `app.js` ↔ `app-dev.js` mirror kept in sync. No A2P file changes. | [Audit #04](./GROOVELINX_REALITY_AUDIT_04_PLAYER_ARCHITECTURE.md) §3 (Spotify API access map) + §7.7 (chokepoint quick win) + user-reported North Star "Loading..." bug |

---

## Audit philosophy

1. **Inventory before fixing.** Each audit produces a report. No fixes during audit.
2. **No deletion.** Even verified-dead code stays on disk until a subsequent Simplify pass confirms removal.
3. **Plan in writing.** Top 10 priorities + first 3 next audits + first 3 do-not-touch from each report.
4. **Cite evidence.** File:line for every claim. Grep counts for every duplicate.
5. **Severity is honest.** Critical / High / Medium / Low — not all findings are equal.

---

## Status conventions

- **LIVE** — wired into UI, regularly executed.
- **HALF-BUILT** — registered but interior is stubs / TODO markers.
- **DUPLICATE** — competes with a canonical implementation.
- **LEGACY** — older path supplanted but not yet removed.
- **DEAD** — provably not loaded or called.
- **FROZEN** — intentionally untouchable for a stated reason (e.g., A2P review).

---

## Next recommended audit / action

With #01, #02, and #03 complete, the audit phase has produced a defensible ownership picture. The next decision is **audit vs. act**:

- **Act path:** start working through the convergence candidates from #03 §7. Highest-value, lowest-risk first:
  - **C6 — per-route lifecycle hook in `showPage()`** closes 4 of the 5 leak risks at the framework level. M effort, HIGH value.
  - **C4 — status badge component enforcement** is a mechanical refactor of 7 inline shadows. S effort, MED value.
  - **C2 — `GLStore.RehearsalSession`** is the largest data-ownership conflict's solution. L effort, HIGH value.

- **Audit path:** continue to #04 (Listener Lifecycle) for the exhaustive interval/rAF inventory, then #05 (Module Decomposition) for the 5-file split criteria. Both are planning work, not user-visible.

Recommend **C6 first** — closing the lifecycle leaks unblocks safer iPhone behavior immediately and provides the hook that Tier-2/3 convergence will rely on.

---

## Doc cross-references

- **Current build & state:** `02_GrooveLinx/CURRENT_PHASE.md`
- **Session history:** `02_GrooveLinx/CLAUDE_HANDOFF.md`
- **Resolved bugs:** `02_GrooveLinx/notes/uat_bug_log.md`
- **Open bugs:** `02_GrooveLinx/uat/bug_queue.md`
- **High-level repo map:** `02_GrooveLinx/PROJECT_INDEX.md`
- **Architectural rules:** `CLAUDE.md` (root)

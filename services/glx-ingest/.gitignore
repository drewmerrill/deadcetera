# Python virtualenv — machine-specific, recreate with:
#   python3 -m venv venv && ./venv/bin/pip install boto3
venv/

# Local credential cache (never created, but ignored as belt-and-suspenders)
.env
*.credentials

{
  "name": "deadcetera",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "npx playwright test",
    "test:desktop": "npx playwright test --project=desktop",
    "test:mobile": "npx playwright test --project=iphone",
    "test:headed": "npx playwright test --headed --project=desktop",
    "test:browserstack": "npx playwright test --config=browserstack.config.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
